import { CurrencyPePipe } from './currency-pe.pipe';

describe('CurrencyPePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyPePipe();
    expect(pipe).toBeTruthy();
  });
});
