server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Logs
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Configuración de gzip
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;

    # ==========================================
    # PRODUCCIÓN: /cotizaciones
    # ==========================================
    location = /cotizaciones/ {
        alias /usr/share/nginx/html/cotizaciones/;
        index index.html;
        try_files $uri /cotizaciones/index.html;

        # Headers de seguridad
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
    }

    location = /cotizaciones {
        alias /usr/share/nginx/html/cotizaciones/index.html;

        # Headers de seguridad
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
    }

    location /cotizaciones/ {
        alias /usr/share/nginx/html/cotizaciones/;
        try_files $uri $uri/ =404;

        # Cache para assets estáticos
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # ==========================================
    # DESARROLLO: /cotizacionesDESA
    # ==========================================
    location = /cotizacionesDESA/ {
        alias /usr/share/nginx/html/cotizacionesDESA/;
        index index.html;
        try_files $uri /cotizacionesDESA/index.html;

        # Headers de seguridad
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
    }

    location = /cotizacionesDESA {
        alias /usr/share/nginx/html/cotizacionesDESA/index.html;

        # Headers de seguridad
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
    }

    location /cotizacionesDESA/ {
        alias /usr/share/nginx/html/cotizacionesDESA/;
        try_files $uri $uri/ =404;

        # Cache para assets estáticos
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Deshabilitar acceso a archivos ocultos
    location ~ /\. {
        deny all;
    }
}

